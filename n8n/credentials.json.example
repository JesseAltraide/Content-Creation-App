{
  "_comment": "Real credential IDs from your n8n instance. Copy to credentials.json and fill in. Open any already-mapped workflow in n8n, click Download, and read the `credentials` block on any HTTP Request node. These are references, not secrets.",
  "supabaseApi": { "id": "PASTE_ID_HERE", "name": "Supabase account" },
  "anthropicApi": { "id": "PASTE_ID_HERE", "name": "Anthropic account" }
}
